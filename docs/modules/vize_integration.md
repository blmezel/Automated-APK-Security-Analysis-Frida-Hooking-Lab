# Vize Cross-Over: Capstone Disassembler Engine
Ported architectural patterns from `Vaultwarden-Reverse-Engineering-Analysis` to examine compiled machine language strings inside native android translation artifacts (`.so` binaries) at the micro-instruction plane (x86_64).
